%h2 Adicionar Membro

= form_for @member do |f|
  = if @member.errors.any?
    #error
      %h4= pluralize(@member.errors.count, "error")
      não permitiram que este membro fosse adicionado.

      %ul
        = @member.errors.full_messages.each do |msg|
          %li= msg

  .field
    f.label :name
    f.text_field :name

    f.label :bio
    f.text_field :bio

    f.label :email
    f.email_field :email
